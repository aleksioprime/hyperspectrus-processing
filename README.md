# hyperspectrus-processing

Репозиторий для разработки алгоритма обработки HyperSpectRus

## Начало работы

```bash
git clone https://github.com/aleksioprime/hyperspectrus-processing
cd hyperspectrus-processing
uv sync --all-packages
```

Расчёт живёт в
[`packages/hsr-proc-algo/src/hsr_proc_algo/processor.py`](packages/hsr-proc-algo/src/hsr_proc_algo/processor.py). Имя пакета, модуль, точка входа и имя реализации `algo`
согласованы с рабочим местом - менять их не нужно, всё остальное можно.

```bash
uv run python -m hsr_proc                         # пакет виден приложению
uv run hsr-proc-check hsr-proc-algo               # приёмка: 24 проверки и вердикт
uv run hsr-proc-run --synthetic --out out         # карты и metrics.json
```

Здесь лежит тот самый расчёт, который сегодня работает в рабочем месте врача:
оптическая плотность, решение системы по матрице коэффициентов, карта THb,
выделение очага порогом Отсу и коэффициент s. Он проходит приёмку целиком, и
это отправная точка исследования: любое изменение сразу видно на фоне рабочего
поведения. Переписывать всё сразу не нужно - можно заменить сегментацию,
оставив восстановление концентраций, и наоборот.

## Структура

| Каталог | Назначение |
|---|---|
| [`packages/hsr-proc-algo`](packages/hsr-proc-algo) | Единственный редактируемый пакет алгоритма |
| [`packages/hsr-proc-base`](packages/hsr-proc-base) | Главный исходник контракта Python и реестра реализаций |
| [`conformance`](conformance) | Инструменты разработки и приёмки; в рабочее приложение не входят |
| [`references`](references) | Рабочая конфигурация спектров и матрица перекрытий; отсюда передаётся в `hyperspectrus` |
| [`datasets`](datasets) | Учебные серии и правила для тестовых данных |
| [`tools`](tools) | Передача контракта, алгоритма и конфигурации в приложение |

## Контракт обработки

Главный исходник контракта находится здесь, в `packages/hsr-proc-base`.
Исследователи могут менять состав входа и результата вместе с алгоритмом и
приёмочными проверками. Такое изменение требует поднять версию пакета, после
чего контракт передаётся в приложение:

```bash
uv run python tools/sync_contract.py ../hyperspectrus
```

В `hyperspectrus` находится зеркало с контрольными суммами. Его не редактируют
вручную: приложение либо адаптируется к новой версии контракта, либо продолжает
использовать предыдущую согласованную версию.

Прибор снимает один участок кожи в нескольких спектрах: геометрия кадра одна,
длины волн разные. Рабочее место передаёт спектральный куб и матрицу
коэффициентов поглощения хромофоров.

Алгоритм должен восстановить карты концентраций, вычислить карту общей
концентрации гемоглобина (THb), выделить область поражения и вернуть средние THb
в поражении и коже вместе с их отношением. Сохранением файлов, базой данных и
интерфейсом занимается рабочее место.

### Реализация

Реализация — объект с тремя членами:

```python
class AlgoProcessor:
    name = "algo"
    version = "0.1"

    def process(self, request, progress=None):
        ...
        return ProcessingResult(...)
```

Это протокол `hsr_proc.Processor`; наследоваться от специального класса не
нужно. `name` должно оставаться равным `algo`, а `version` поднимается при любом
изменении расчёта.

#### Вход

`ProcessingRequest` содержит:

| Поле | Содержание |
|---|---|
| `cube` | `SpectralCube`: массив `(спектры, высота, ширина)` типа `float32`, значения от 0 до 1 и длины волн в порядке кадров |
| `overlap` | `OverlapMatrix`: строки соответствуют длинам волн, столбцы — хромофорам |
| `params` | `ProcessingParams`: общий `gaussian_sigma` и словарь `extra` для параметров алгоритма |

Порядок строк матрицы может отличаться от порядка слоёв куба. Перед расчётом
обязательно вызывайте:

```python
matrix = request.overlap.aligned_to(request.cube)
```

Метод одновременно переставляет строки и проверяет наличие коэффициентов для
всех снятых длин волн.

#### Выход

Нужно вернуть `ProcessingResult` со всеми полями:

| Поле | Содержание |
|---|---|
| `concentrations` | Карта `float32` размером с кадр для каждого хромофора справочника |
| `thb_map` | Карта общей концентрации гемоглобина `float32` |
| `lesion_mask` | Маска поражения типа `bool` |
| `metrics` | Коэффициент s и средние THb в поражении и коже |
| `segmentation` | Метод сегментации, порог и использованное сглаживание |
| `processor` | Ровно `f"{self.name} {self.version}"` |
| `notes` | Допущения и предупреждения для врача |

Размер всех карт совпадает с размером кадра. NaN и бесконечные значения не
допускаются.

### Подключение пакета

Рабочее место находит алгоритм по точке входа, а не прямым импортом:

```toml
[project]
name = "hsr-proc-algo"
requires-python = ">=3.11"
dependencies = [
    "hsr-proc-base>=0.1,<0.2",
    "numpy>=2.1",
]

[project.entry-points."hsr_proc.processors"]
algo = "hsr_proc_algo:AlgoProcessor"
```

Без этой записи реализация не появится в рабочем месте. Точка входа уже
настроена в пакете и обычно не требует изменений.

### Приёмочные проверки

Пакет [`hsr-proc-conformance`](conformance) используется только при разработке
и приёмке. Он предоставляет две команды:

- `hsr-proc-check` проверяет установленный пакет и формирует вердикт;
- `hsr-proc-run` запускает алгоритм на синтетической или файловой серии без
  рабочего места.

Уровни проверки:

| Уровень | Что проверяет |
|---|---|
| `contract` | Форму результата, типы, отсутствие NaN, неизменность входа, отсутствие файлового и сетевого ввода-вывода |
| `numeric` | Концентрации, THb, сегментацию и показатели на синтетических данных с известным ответом |
| `budget` | Время обработки кадра прибора |

```bash
uv run pytest
uv run pytest -m contract packages/hsr-proc-algo/tests
uv run hsr-proc-check hsr-proc-algo
uv run hsr-proc-check hsr-proc-algo --report приёмка.md
```

#### Другая математическая модель

Синтетическая проверка концентраций основана на линейной модели. Для алгоритма,
учитывающего рассеяние или использующего обученную модель, допустимо объявить
отступление в
[`packages/hsr-proc-algo/src/hsr_proc_algo/conformance.toml`](packages/hsr-proc-algo/src/hsr_proc_algo/conformance.toml):

```toml
profile = "custom"
concentration_atol = 1e-3
deviations = [
    "Модель учитывает рассеяние, точное обращение линейной системы не воспроизводится",
]
```

Файл лежит внутри пакета намеренно: он едет вместе с алгоритмом и читается и
здесь, и на приёмке. В `pyproject.toml` его держать нельзя - тот в передачу не
попадает, и приёмка взяла бы настройки чужого репозитория, а объявленные
отступления исчезли бы вместе с причинами.

Каждое ослабление допуска должно быть объяснено: проверка
`test_послабления_объяснены` требует непустой `deviations`. Уровень `contract`,
выделение очага и коэффициент s остаются обязательными при любом профиле.

### Передача в приложение

Перед передачей:

1. Добейтесь успешного `uv run hsr-proc-check hsr-proc-algo --report приёмка.md`.
2. Поднимите `AlgoProcessor.version` при любом изменении расчёта. Версия попадает
   в карточку сеанса и только растёт: в приложении сейчас работает `algo 1.0`.

Пакет переносится зеркалом, как контракт:

```bash
uv run python tools/sync_algo.py ../hyperspectrus
```

Колесом передавать нельзя, хотя `uv build --package hsr-proc-algo` и соберёт
его: в приложении пакет с тем же именем подключён членом workspace, и ближайший
`uv sync` вернул бы прежний расчёт - молча, вместе с собранной версией
программы.

Рядом с зеркалом появляется `MIRROR.json` с коммитом источника и суммами
файлов, и тест приложения не даёт править переданный пакет на месте. Дальше
издатель повторяет приёмку у себя, собирает полную версию рабочего места и
публикует её. Отдельно доустановить алгоритм в поставленное врачу приложение
нельзя.

## Тестовые данные

Синтетические серии создаются автоматически. Сгенерированный файловый пример
лежит в `datasets/example/`, а настоящие кадры кладутся в `datasets/real/jpeg/`
по правилам из [`datasets/README.md`](datasets/README.md) и не фиксируются в
Git. Снимки пациентов не должны попадать в этот репозиторий.

Рабочая конфигурация `HSR-EXAMPLE` хранится в
[`references/hsr-example.json`](references/hsr-example.json). В отличие от
контракта, здесь главным является этот репозиторий: исследователи подбирают
коэффициенты, меняют `reference_version`, проверяют алгоритм и передают точную
копию в приложение:

```bash
uv run python tools/sync_reference.py ../hyperspectrus
```
